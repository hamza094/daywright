<?php

declare(strict_types=1);

return [
    /*
     |--------------------------------------------------------------------------
     | Back to system URL override
     |--------------------------------------------------------------------------
     | Point the "Back to {{ app.name }}" link to the admin panel.
     | Use a relative path so it works across environments/domains.
     */
    'back_to_system_url' => '/admin/panel',

    /* Optional: change the label shown in the UI */
    'back_to_system_label' => 'Back to DayWright',
];
